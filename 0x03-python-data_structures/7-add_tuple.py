#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    my_tuple = ()
    len_a = len(tuple_a)
    len_b = len(tuple_b)
    for a in tuple_a:
        for b in tuple_b:
            if len_a == len_b:
                 my_tuple +=  (a + b)
            if len_b > len_a:
                if b.index > c.index:
                    my_tuple += (b + 0)
                else:
                    my_tuple +=

                my_tuple + = (0 + b)
        print("The tuple after addition is: ")
    print(new_tuple)
