#!/usr/bin/python3
"""
Defines is_kind_of_class(obj, a_class).

"""


def is_kind_of_class(obj, a_class):
    """It return True if the objects is an instance, otherwise False"""

    return (isinstance(obj, a_class))
